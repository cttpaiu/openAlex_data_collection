---
title: Configuration
description: "Every key in `config/collection.yml`, every flag, every override."
---

:::caution
This page is a stub. Full content lands in the next docs commit.
:::

Every key in `config/collection.yml`, every flag, every override.

---
title: export-format
description: "Registered placeholder command for future CSV exports."
---

## File

`openalex/commands/export.py`

## Current behavior

`export_format_command()` is a Click command with no options. It only prints a Rich warning that export formatting is not implemented yet.

## Implementation note

Because this command has no data path today, docs should describe it as a placeholder until the export schema and target CSV formats are added.
